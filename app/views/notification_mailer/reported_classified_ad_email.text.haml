Bonjour #{@classified_ad.user.display_name}

Votre annonce a été signalée pour la raison suivante :
#{@reject_reason}

Notre équipe est en train de vérifier le contenu incriminé. Voici l'annonce concernée :

- if @classified_ad.title.present?
  = @classified_ad.title
= l @classified_ad.created_at, format: :short

= @content

Voir l'annonce : #{@classified_ad_path}
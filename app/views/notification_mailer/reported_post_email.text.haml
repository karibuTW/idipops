Bonjour #{@post.user.display_name}

Votre post a été signalé pour la raison suivante :
#{@reject_reason}

Notre équipe est en train de vérifier le contenu incriminé. Voici le post concerné :

- if @post.title.present?
  = @post.title
= l @post.created_at, format: :short

= @content

Voir le post : #{@post_path}
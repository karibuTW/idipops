Bonjour #{@user_name}

Votre offre n'a pas été acceptée pour la raison suivante :
= @reject_reason

Voici l'annonce concernée :
- if @conversation.classified_ad.title.present?
  = @conversation.classified_ad.title

= l @conversation.classified_ad.created_at, format: :short

= @content

Voir l'annonce : #{@classified_ad_path}